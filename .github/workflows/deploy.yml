# name: Deploy to Amplify

# on:
#     pull_request:
#         branches:
#             - main # Change this to your default branch

# jobs:
#     deploy:
#         runs-on: ubuntu-latest

#         steps:
#             - name: Checkout code
#               uses: actions/checkout@v2

#             - name: Set up Node.js
#               uses: actions/setup-node@v2
#               with:
#                   node-version: '16' # Use the version your app requires

#             - name: Install dependencies
#               run: npm install

#             - name: Build application
#               run: npm run build

#             - name: Deploy to AWS Amplify
#               env:
#                   AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
#                   AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#                   AWS_REGION: 'us-east-1' # e.g., us-east-1
#               run: |
#                   # Install Amplify CLI if not already available
#                   npm install -g @aws-amplify/cli

#                   # Configure Amplify
#                   amplify configure project --yes

#                   # Deploy the app
#                   amplify publish --yes

# name: Deploy to Amplify

# on:
#     pull_request:
#         branches:
#             - main

# jobs:
#     deploy:
#         runs-on: ubuntu-latest

#         steps:
#             - name: Checkout code
#               uses: actions/checkout@v2

#             - name: Set up Node.js
#               uses: actions/setup-node@v2
#               with:
#                   node-version: '16'

#             - name: Install dependencies
#               run: npm install

#             - name: Install Amplify CLI
#               run: npm install -g @aws-amplify/cli

#             - name: Build application
#               run: npm run build

#             - name: Deploy to AWS Amplify
#               env:
#                   AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
#                   AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#                   AWS_REGION: 'us-east-1'
#               run: |
#                   echo "Deploying to Amplify..."
#                   amplify pull --appId di37olohmms1l --envName dev --yes
#                   amplify publish --branch ${{ github.head_ref }} --yes

name: Deploy React App to GitHub Pages

on:
    pull_request:
        branches:
            - main

jobs:
    build-and-deploy:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v2

            - name: Set up Node.js
              uses: actions/setup-node@v2
              with:
                  node-version: '16' # or any version you are using

            - name: Install dependencies
              run: npm install

            - name: Build the app
              run: npm run build

            - name: Deploy to GitHub Pages
              uses: peaceiris/actions-gh-pages@v3
              with:
                  github_token: ${{ secrets.GH_DEPLOY_TOKEN }}
                  publish_dir: ./build
                  publish_branch: gh-pages
